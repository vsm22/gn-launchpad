enum EventType {
    push = "push",
    release = "release",
    hold = "hold",
    holdRelease = "holdRelease",
    longHold = "longHold",
    longHoldRelease = "longHoldRelease",
    doubleTap = "doubleTap",
    midiEvent = "midiEvent"
}

export default EventType;