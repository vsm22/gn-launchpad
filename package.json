{
  "name": "gn-launchpad",
  "version": "1.0.0",
  "description": "Launchpad mapping for Max",
  "main": "gn-launchpad.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist/* && tsc && cp src/max/gn-launchpad.maxpat dist && mkdir -p dist/config && cp -r src/config/* dist/config",
    "watch": "tsc --watch"
  },
  "author": "vsm22",
  "license": "ISC",
  "devDependencies": {
    "readline": "^1.3.0",
    "tslint": "^5.20.1",
    "typescript": "^3.7.3"
  },
  "dependencies": {
    "@types/node": "^12.12.21",
    "@types/webmidi": "^2.0.4"
  }
}
