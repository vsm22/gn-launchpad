{
  "name": "gn-launchpad",
  "version": "1.0.0",
  "description": "Launchpad mapping for Max",
  "main": "gn-launchpad.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -rf dist/* && npm run webpack && cp src/max/gn-launchpad.maxpat dist/max && mkdir -p dist/max/config && cp src/config/* dist/max/config && mkdir -p dist/web/config && cp src/config/* dist/web/config",
    "webpack": "webpack",
    "watch": "tsc --watch"
  },
  "author": "vsm22",
  "license": "ISC",
  "devDependencies": {
    "readline": "^1.3.0",
    "ts-loader": "^6.2.1",
    "tslint": "^5.20.1",
    "typescript": "^3.7.3",
    "webpack": "^4.41.6",
    "webpack-cli": "^3.3.11"
  },
  "dependencies": {
    "@types/node": "^12.12.21",
    "@types/webmidi": "^2.0.4",
    "file-system": "^2.2.2",
    "fs": "0.0.1-security"
  }
}
